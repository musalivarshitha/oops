class Car:
    def __init__(self, brand, model=None):
        if model:
            self.__init__(brand)   # calling another constructor
        self.brand = brand
        self.model = model

c = Car("Toyota", "Corolla")
print(c.brand, c.model)  # Output: Toyota Corolla
